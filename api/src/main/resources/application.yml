server:
  port: 8090

spring:
  jackson:
    serialization:
      write-dates-as-timestamps: false

logging:
  level:
    org:
      springframework:
        security: TRACE
    com:
      okta:
        spring: DEBUG

okta:
  oauth2:
    issuer: https://dev-4569717.okta.com/oauth2/default
    #audience: api://default